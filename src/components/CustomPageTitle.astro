---
const { title, author } = Astro.props.entry.data;
---

<h1 id="_top">{title}</h1>
{author && (
  <em>Author: {author}</em>
)}

<style>
  h1 {
    margin-top: 1rem;
    font-size: var(--sl-text-h1);
    line-height: var(--sl-line-height-headings);
    font-weight: 600;
    color: var(--sl-color-white);
  }
</style>
